# python-otel-app

Simple Flask application prepared for SkyWalking agent integration.

## Running

Install dependencies and start the server:

```bash
pip install -r requirements.txt
python main.py
```

Endpoints:
- `/` returns plain text
- `/json` returns JSON
- `/html` returns HTML
